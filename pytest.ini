[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
# On Windows, ProactorEventLoop.socketpair() binds to 127.0.0.1.
# Allow localhost so the event loop can initialize while still blocking real network calls.
allow_hosts = 127.0.0.1,localhost,::1
